on:
  workflow_dispatch:
  push:
    branches: main

name: Quarto Publish

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render and Publish
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}



name: Publish HTML Website

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# jobs:
#   build-website:
#     runs-on: ubuntu-latest
# 
#     steps:
#       # Checkout repository
#       - uses: actions/checkout@v3
# 
#       # ---------- Install system dependencies ----------
#       - name: Install system libraries
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y \
#             libudunits2-dev \
#             libgdal-dev \
#             libgeos-dev \
#             libproj-dev \
#             libsqlite3-dev \
#             libcurl4-openssl-dev \
#             libssl-dev \
#             libxml2-dev \
#             libfontconfig1-dev \
#             libfreetype6-dev \
#             libharfbuzz-dev \
#             libfribidi-dev \
#             libgit2-dev \
#             gdal-bin \
#             proj-bin
# 
#       # Install Quarto
#       - uses: quarto-dev/quarto-actions/setup@v2
# 
#       # Install R
#       - uses: r-lib/actions/setup-r@v2
# 
#       # Setup renv
#       - uses: r-lib/actions/setup-renv@v2
# 
#       # Restore project packages from renv.lock
#       - name: Restore R packages
#         run: R -e 'renv::restore(prompt = FALSE)'
# 
#       # Cache project-local renv library (after restore)
#       - name: Cache renv packages
#         uses: actions/cache@v3
#         with:
#           path: renv/library
#           key: ${{ runner.os }}-renv-${{ hashFiles('renv.lock') }}
#           restore-keys: |
#             ${{ runner.os }}-renv-
# 
#       # Install sn globally so INLA can detect it
#       - name: Install sn globally
#         run: R -e 'install.packages("sn", repos="https://cloud.r-project.org/")'
# 
#       # Render ONLY HTML website (NO PDF)
#       - name: Render HTML website
#         run: quarto render --to html
# 
#       # Deploy to GitHub Pages
#       - name: Deploy to gh-pages
#         uses: peaceiris/actions-gh-pages@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           publish_dir: _site
